apiVersion: v1
kind: Service
metadata:
  name: peteq-api
spec:
  selector:
    app: peteq-api
  ports:
  - port: 80 
    name: web
    targetPort: 8080
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: peteq-api
spec:
  selector:
    matchLabels:
      app: peteq-api
  template:
    metadata:
      labels:
        app: peteq-api
    spec:
      volumes: []
      containers:
      - name: peteq-api
        imagePullPolicy: Always
        args:
          - "start"
          - "server"
        image: peteqproj/peteq 
        env:
        - name: EVENTBUS_TYPE
          valueFrom:
            configMapKeyRef:
              key: eventbus_type
              name: configmap
        - name: COMMANDBUS_TYPE
          valueFrom:
            configMapKeyRef:
              key: commandbus_type 
              name: configmap
        - name: PORT
          value: "8080"
        - name: POSTGRES_URL
          valueFrom:
            configMapKeyRef:
              key: postgres_url
              name: configmap
        resources:
          limits:
            memory: "128Mi"
            cpu: "500m"
        volumeMounts: []
        ports:
        - containerPort: 8080
